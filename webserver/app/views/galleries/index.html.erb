
<div class="page-header">
  <%= link_to 'Back', root_path, :class => "btn btn-default" %>
  <h1>Public Galleries</h1>
</div>

<table>
  <tr>
    <th>Gallery Name</th>
  </tr>
  <% if @public_galleries %>
	  <% @public_galleries.each do |gallery| %>
	    <tr>
	      <td><%= link_to gallery.name, room_gallery_path(gallery.room, gallery) %></td>
	    </tr>
	  <% end %>
  <% end %>
</table>
